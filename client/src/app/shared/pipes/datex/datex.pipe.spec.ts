/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DatexPipe } from './datex.pipe';

describe('Pipe: Datexe', () => {
  it('create an instance', () => {
    let pipe = new DatexPipe();
    expect(pipe).toBeTruthy();
  });
});
